OutboxMessage[] - new List<OutboxMessage>()
//временное хранилище для сообщений отправляемых в очередь, был массив - стал список, использованы методы LINQ - вместо индексации

Trademark[] - new List<Trademark>()
//разделение списка товарных знаков на части для параллельной обработки, был массив - стал список, использован метод LINQ - ChunkBy

str[0], str[1], str[3] .... - new StringBuilder().AppendLine()
//отправка сообщения в чат мониторинга ошибок, было обращение напрямую по индексам массива, стало использование StringBuilder для построения строки

//проход по матрице для работы со словарями (внутренние словари для работы продукта) заменен на Dictionary<string, List> 
//для более простого доступа по конкретному коду словаря

//замена шаблонных картинок в docx файле, для картинок находящихся внутри таблиц приходилось проходится в таком виде table.Rows[i].Cells[j]
//удалено, поскольку оказалось уже не нужно:)

  
