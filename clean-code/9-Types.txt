WEIGHT_DIMENSION = вес
//было строка, стало  константа измерения весом при работе с договором перевозки

EUROPALLET_DIMENSION = европаллет
//было строка, стало  константа измерения европаллетами при работе с договором перевозки

CAPACITY_DIMENSION = объем
//было строка, стало  константа измерения объема при работе с договором перевозки

IsDaysOutOfMonth(int days) => days > 30;
//было повторяющее условие, стало функция проверяющая количество дней в месяце, более 30 дней нужно именно для договора перевозки, так что это нормальный кейс проверки.

isLegalEntityContragent
//было проверка является ли контрагент юридическом лицом без использования бул переменной, стало добавилась бул переменная

было statement.Status == StatementStatus.Collecting || statement.Status == StatementStatus.Cancelled - canAddComment
//условие для возможности добавления комментарий, стало бул переменная

//переделано хранение максимального веса ТС было string, стало int, приводилось строка в int Для проверок

//переделано хранение года выхода ТС было string, стало int, приводилось строка в int Для проверок

//проверка грузоподъемности не было проверки если получилось число меньше 0, добавил проверку

//было float переменная разности расстояния для перевозки, стало double

isCombinedType || isGraphicType
//было text.StartsWith($"({557})" || text.StartsWith($"({558})", стало бул переменная является ли текущая строка изобразительным или комбинированным знаком

//было types.Contains(EventType.NewPassIssued) || types.Contains(EventType.PassAnnulled), повторялось 3 раза
//стало бул переменная isNewOrAnnulledPass


 