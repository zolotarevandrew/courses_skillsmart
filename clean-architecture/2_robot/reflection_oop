По ООП стилю

Плюсы:
- Вся логика инкапсулирована
- Робот знает как и какие команды ему исполнять

Минусы:
- Logger лучше передавать в конструктор, для простой замены, проще было передать делегат(функцию)
- Состояние мутабельно, нет видимого перехода из состояния в состояние
