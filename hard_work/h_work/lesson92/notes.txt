1) Monoid

Example1 - предикаты для фильтрации по-условию, коллекции в памяти.
Example2 - расширение примера 1, только для построения глупого where and условия в sql..

2) Group + Ring

Example 1 - транзакции и возможность отката.
Example 2 - модульное кольцо, например для циклических буферов или времени.

Итог — самым сложным было придумать примеры использования, поскольку группы, моноиды и кольца — это очень абстрактные понятия. 
В целом, требуется более глубокое погружение в тему.

(Очень много лишних аллокаций выходит за счет функционального стиля и нужно дополнительно изворачиваться, чтобы все закэшировать (чего я не делал).
В Fold, очень легко получить boxing (если передавать и пытаться инстанциировать M monoid = default, убрал дефолтные параметры), 
Благо компилятор добавляет в IL код constrained перед callvirt, то есть вызывает методы структуры напрямую.)
