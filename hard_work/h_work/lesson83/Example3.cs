namespace h_work.lesson83;

public class Example3
{
    /*
     private async Task<string> GetFormattedPhoneNumber(string phoneNumber, Guid userId)
        {
            var isEnabled = await _featureToggleHelper.IsAvailableAsync(EFeatureCode.RemovePhoneFirstZero, userId);
            return isEnabled && phoneNumber.StartsWith("0") && phoneNumber.Length > 1 
                ? phoneNumber[1..] 
                : phoneNumber;
        }
     */
}