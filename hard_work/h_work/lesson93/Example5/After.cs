namespace h_work.lesson93.Example5;

public class After
{
    /*
      public async Task<CompanyBusinessInfoResponse> Get(Guid companyId)
      {
     *   var company = await _service.Get(contract.CompanyId);
        if (company == null)
        {
            return new CompanyBusinessInfoResponse 
            {
                NotFound = true,
                Value = null,
            }
        }

        var response = new CompanyBusinessInfoResponse
        {
            NotFound = false,
            Value = new
            {
                Id = company.Id,
            }
        };
        
        return response;
     * }
     */
}