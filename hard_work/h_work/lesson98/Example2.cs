namespace h_work.lesson98;

public class Example2
{
    /*
     *  public async Task<IEnumerable<DictInternalTsTypeRowDto>> QueryInternalTsTypeAsync(string search)
        {
            var repo = _byCodeRepository.Get(DictConsts.InternalTsType);
            var rows = await repo.SearchAsync(search);
            var r = rows.Select(r => r as DictInternalTsTypeRow).ToList();
            return r.Select(r => r.ToDto()).ToList();
        }
     */
}