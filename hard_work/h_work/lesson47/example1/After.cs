namespace h_work.lesson47.example1;

/*
public class After
{
    public async Task Test()
    {
        var triggers = new List<FillerPersonTrigger>
        {
            new RegistratorOnlyOneLrWithUnknownRepresentationTrigger(_slackService, _inhouseLinkGetter),
            new RegistratorOnlyOneLrWithUnknownRepresentationAndOtherPersonsTrigger(_slackService, _inhouseLinkGetter),
            new ManyLrsAndRegistratorUnknownRepresentationTrigger(_slackService, _inhouseLinkGetter),
        };

        foreach (var trigger in triggers)
        {
            await trigger.CheckAsync(context, persons, owner);
            if (trigger.Triggered)
            {
                break;
            }
        }
    }
}
*/