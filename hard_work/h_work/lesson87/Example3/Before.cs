namespace h_work.lesson87.Example3;

public class Before
{
    public void Main()
    {
        /*
         *    string phoneCode = contract.PhoneCode?.TrimStart('+');
            string phoneNumber;

            (phoneCode, phoneNumber) = MobilePhoneHelper.ValidateAndNormalizeV2(phoneCode.TrimStart('+'), contract.PhoneNumber);
         */
    }
}