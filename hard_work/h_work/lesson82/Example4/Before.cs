namespace h_work.lesson82.Example4;

public class Before
{
    /*
     * public void Schedule(DateTime date)
     * {
     *     
     *      var now = DateTime.UtcNow;
     *      if (now <= now.AddMinutes(5)) throw new InvalidOperationException("invalid date");
     *      if (now >= now.AddDays(5)) throw new InvalidOperationException("invalid date");
     *
     *      var expireTime = date - now;
     
     * }
     */
}