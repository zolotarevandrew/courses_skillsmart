"InitializeGame() -- это начальная инициализация игрового пространства (случайное заполнение игровой доски).
Напишите словами три-четыре последовательных шага, как вы будете это делать (логика реализации)"

1) Берем дефолтный размер доски - 8 (DefaultSize), и выносим в константу, рефакторим метод Draw на использование константы.
2) Создаем новый Board размером DefaultSize.
3) Во вложенном цикле по size, заполняем элементы рандомными значениями. 
Генерируя случайное число [0..symbols.Length), получаем индекс и создаем новый Element.

